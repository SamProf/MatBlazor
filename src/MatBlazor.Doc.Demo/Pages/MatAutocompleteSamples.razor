@page "/Autocomplete";
@attribute [RouteDisplay( "MyGroup","My AutoComplete", false)]
<XmlDocContainer ComponentType="@(typeof(MatAutocomplete<,>))" DocComponentType="@this.GetType()" />

<MatH3>This component is in progress, use MatAutocompleteList instead!</MatH3>


<RazorDocContainer DocInfo="@this.GetDocInfo("Basic Example")">
    <MatAutocomplete Items="@options" Label="Pick one" @bind-Value="@value"></MatAutocomplete>
    @code
        {

        string value;

        string[] options = new[]
        {
                "One",
                "Two",
                "Three"
            };

    }
</RazorDocContainer>

