@inherits MatBlazor.Demo.Components.BaseDocComponent

@* THIS FILE IS AUTOGENERATED FROM C# XML Comments! *@
@* ALL MANUAL CHANGES WILL BE REMOVED! *@


@if (!Secondary) {<h3 class="mat-h3">MatTreeView</h3> } else { <h5 class="mat-h5">MatTreeView</h5> }

<p>Renders the data as a tree.</p>

<div><table class="article-table mat-elevation-z5 mdc-theme--surface">
	<tr>
		<th>Name</th>
		<th>Type</th>
		<th>Description</th>
	</tr>
	<tr>
		<td style="font-weight: bold;">TNode</td>
		<td>Generic argument</td>
		<td></td>
	</tr>
	<tr>
		<td>ExpandStateChanged</td>
		<td>EventCallback&lt;ExpandedStateChangedArgs&lt;TNode&gt;&gt;</td>
		<td>An event raised when the expanded state of a node changes</td>
	</tr>
	<tr>
		<td>GetChildNodesCallback</td>
		<td>GetChildNodesDelegate&lt;TNode&gt;</td>
		<td>A function that gets all the child nodes for a given parentNode.<br/>If lazy loading is supported and the child nodes have not yet been<br/>loaded then it should return null.<br/>(Required)</td>
	</tr>
	<tr>
		<td>IsNodeExpandedCallback</td>
		<td>IsNodeExpandedDelegate&lt;TNode&gt;</td>
		<td>A function that indicates if the given node is expanded or collapsed.<br/>(Optional - if null then the expanded/collapsed state is managed by the )</td>
	</tr>
	<tr>
		<td>LoadChildNodesCallback</td>
		<td>LoadChildNodesDelegate&lt;TNode&gt;</td>
		<td>A function to lazy load child nodes. Only required if lazy loading / load on demand<br/>is used.<br/>(Optional - unless  returns null)</td>
	</tr>
	<tr>
		<td>NodeTemplate</td>
		<td>RenderFragment&lt;TNode&gt;</td>
		<td>The template used to render the Node<br/>(Required)</td>
	</tr>
	<tr>
		<td>RootNode</td>
		<td>TNode</td>
		<td>The root node to be displayed in the tree<br/>One of the following must be supplied  or  (but NOT BOTH)</td>
	</tr>
	<tr>
		<td>RootNodes</td>
		<td>IEnumerable&lt;TNode&gt;</td>
		<td>All the root nodes displayed in the tree<br/>One of the following must be supplied  or  (but NOT BOTH)</td>
	</tr>
	<tr>
		<td>SelectedNode</td>
		<td>TNode</td>
		<td>The selected item in the tree<br/>(Optional - default null)</td>
	</tr>
	<tr>
		<td>SelectedNodeChanged</td>
		<td>EventCallback&lt;TNode&gt;</td>
		<td>An event raised when the selected node changes</td>
	</tr>
</table></div>
